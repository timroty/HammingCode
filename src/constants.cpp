#include "include/constants.hpp"

namespace Constants {
    const char* STR_ENCODE = "encode";
    const char* STR_DECODE = "decode";

    const int INT_CODEWORD_LENGTH = 16;
    const int INT_PARITY_BITS = 4; // Not including extended
    const int INT_DATA_BITS = 11;
}
